# How to run a MySQL database on the computer

1. Download with apt. Activate the process with systemctl
2. Go into the shell and create a database (sudo mysql --> CREATE TABLE IF NOT EXISTS ...)
3. 

ok